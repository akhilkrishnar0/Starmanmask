
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Starmanmask"
version = "0.1.0"
description = "Manually mask stars in galaxy FITS images"
authors = [{name = "Akhil Krishna R"}]
dependencies = ["numpy", "matplotlib", "astropy"]
